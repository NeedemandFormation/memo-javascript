// STRING MANIPULATION
let texte = "Hello World";
texte.length; // Longueur de la chaîne
texte.toUpperCase(); // Convertit en majuscules
texte.toLowerCase(); // Convertit en minuscules
texte.substring(0, 5); // Extrait une portion
texte.split(" "); // Divise en tableau
texte.trim(); // Enlève les espaces début/fin

// TEMPLATE LITERALS
let nom = "Jean";
let message = `Bonjour ${nom}!`; // Interpolation de variables
