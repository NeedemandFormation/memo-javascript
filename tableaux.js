// TABLEAUX (ARRAYS)
// Méthodes courantes
let fruits = ["pomme", "banane", "orange"];
fruits.push("fraise"); // Ajoute à la fin
fruits.pop(); // Retire le dernier élément
fruits.unshift("citron"); // Ajoute au début
fruits.shift(); // Retire le premier élément
fruits.slice(1, 3); // Extrait une portion du tableau
fruits.splice(1, 1); // Retire des éléments
fruits.indexOf("banane"); // Trouve l'index d'un élément
fruits.includes("pomme"); // Vérifie si l'élément existe
